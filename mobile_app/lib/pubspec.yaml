name: mobile_app
description: "QNV 2030 Cardio-Neural Digital Twin Mobile Console"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
dependencies:
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
dependencies:
  fl_chart: ^0.66.0
